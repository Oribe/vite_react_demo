import { Layout } from "antd";
import React, { FC } from "react";

const AFooter = Layout.Footer;

const Footer: FC = () => {
  return <AFooter style={{ background: "transparent" }} />;
};

export default Footer;
